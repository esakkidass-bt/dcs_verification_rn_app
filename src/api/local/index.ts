import {default as tables} from './tables';
import {default as landDetails} from './landDetails';
import {default as ownerDetails} from './ownerDetails';
import {default as cropSurvey} from './cropSurvey';

const local = {
  landDetails,
  ownerDetails,
  tables,
  cropSurvey,
};

export default local;
