import {default as getCropTypes} from './cropTypes';
import {default as getCropClassifications} from './cropClassifications';
import {default as getCrops} from './crops';
import {default as getMajorCrops} from './majorCrops';

const cropMaster = {
  getCropTypes,
  getCropClassifications,
  getCrops,
  getMajorCrops,
};

export default cropMaster;
