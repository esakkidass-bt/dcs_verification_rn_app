import {default as irrigationSource} from './irrigationSource';
import {default as croppingMethod} from './croppingMethod';
import {default as cropSeasonType} from './cropSeasonType';
import {default as cropStage} from './cropStage';

const cropMaster = {
  irrigationSource,
  croppingMethod,
  cropSeasonType,
  cropStage,
};

export default cropMaster;
