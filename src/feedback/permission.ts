import {IFeedbackGroup} from '../@types';

const d: IFeedbackGroup = {
  dsd: {
    code: 'PERMISSION',
    type: 'Error',
    message: 'Permission Denied',
    status: 'failed',
  },
};

export default d;
